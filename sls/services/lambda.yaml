macroFunction:
  handler: main.lambda_handler
  name: lambda-function-apse1-${self:custom.stage}-${self:custom.projectName}
  description: Lambda worker to handle the macro template
  runtime: python3.12
  memorySize: 128
  timeout: 600
  runtimeManagement:
    mode: auto
  tags: ${file(sls/tagging/tag.yaml):tags}
